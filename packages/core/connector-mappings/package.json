{
  "name": "@quickdeployai/connector-mappings",
  "version": "0.1.0",
  "private": true,
  "description": "Data-driven connector mapping packs: vendor node/module/action types resolved to capability requirements; unmapped types compile to fidelity-blocked.",
  "files": [
    "dist",
    "src"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@quickdeployai/workflow-ir": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@quickdeployai/workflows-registry-workspace-config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=22.18"
  }
}
