{
  "name": "@quickdeployai/workflow-ir",
  "version": "0.1.0",
  "private": true,
  "description": "ExecutionPlanV1 \u2014 the versioned private execution IR every importer compiles to.",
  "files": [
    "dist",
    "src"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@quickdeployai/workflow-core": "workspace:*",
    "@quickdeployai/workflow-expressions": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@quickdeployai/workflows-registry-workspace-config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=22.18"
  }
}
