{
  "name": "@quickdeployai/workflow-policy",
  "version": "0.1.0",
  "private": true,
  "description": "Three-stage policy model: plan-derived intent, install-time effective policy (reduce-only intersection), per-action runtime decisions. Default-deny.",
  "files": [
    "dist",
    "src"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@quickdeployai/workflow-core": "workspace:*",
    "@quickdeployai/workflow-ir": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@quickdeployai/workflow-expressions": "workspace:*",
    "@quickdeployai/workflows-registry-workspace-config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=22.18"
  }
}
